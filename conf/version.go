//DONOT EDIT THIS FILE
package conf

// GIT_VERSION=$(git log -1 --pretty=format:"commit:  %h %d %s <%an> %ai" --abbrev-commit --graph)
// sed -i "s/verion/$GIT_VERSION/" conf/version.go

const (
	GIT_VERSION = "verion"
)
